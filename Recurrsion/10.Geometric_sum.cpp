// checking an array is sorted or not 
#include<iostream>
using namespace std;
bool check(int a[],int n)
{
if(n==0 || n==1) return true;
if(check(n)>check(n-1);
return true;
else 
return false;    
    
}
int main()
{
cout<<check(12345,5);
return 0;    
}